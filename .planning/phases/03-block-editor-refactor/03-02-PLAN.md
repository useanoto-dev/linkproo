---
phase: 03-block-editor-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/blocks/MediaBlockEditor.tsx
  - src/components/editor/blocks/InteractiveBlockEditor.tsx
  - src/components/editor/blocks/ListBlockEditor.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "MediaBlockEditor renders image, video, spotify, map, html, carousel block types"
    - "InteractiveBlockEditor renders image-button, animated-button, email-capture, countdown, badges block types"
    - "ListBlockEditor renders faq, gallery, testimonial, stats, product, contacts block types"
    - "buttonPresets memo is computed inside InteractiveBlockEditor from block.buttonHeight"
    - "contacts IIFE pattern is preserved verbatim in ListBlockEditor"
  artifacts:
    - path: "src/components/editor/blocks/MediaBlockEditor.tsx"
      provides: "MediaBlockEditor for image, video, spotify, map, html, carousel"
      exports: ["MediaBlockEditor"]
    - path: "src/components/editor/blocks/InteractiveBlockEditor.tsx"
      provides: "InteractiveBlockEditor for image-button, animated-button, email-capture, countdown, badges"
      exports: ["InteractiveBlockEditor"]
    - path: "src/components/editor/blocks/ListBlockEditor.tsx"
      provides: "ListBlockEditor for faq, gallery, testimonial, stats, product, contacts"
      exports: ["ListBlockEditor"]
  key_links:
    - from: "src/components/editor/blocks/InteractiveBlockEditor.tsx"
      to: "src/components/editor/blocks/constants.ts"
      via: "import ANIM_STYLE_OPTIONS"
      pattern: "import.*ANIM_STYLE_OPTIONS.*from.*constants"
    - from: "src/components/editor/blocks/InteractiveBlockEditor.tsx"
      to: "src/components/editor/ImageUploader"
      via: "import buildButtonPresets"
      pattern: "import.*buildButtonPresets.*from.*ImageUploader"
    - from: "src/components/editor/blocks/ListBlockEditor.tsx"
      to: "@/types/smart-link"
      via: "import FaqItem, GalleryImage, StatItem, ContactItem"
      pattern: "import.*FaqItem.*from.*@/types/smart-link"
---

<objective>
Extract the three largest group editors (MediaBlockEditor, InteractiveBlockEditor, ListBlockEditor) from the BlockEditor.tsx monolith into separate files under `src/components/editor/blocks/`.

Purpose: These are the biggest chunks of JSX (~700 lines combined). Extracting them in parallel with Plan 01 maximizes efficiency. BlockEditor.tsx is NOT modified yet — that happens in Plan 03.
Output: 3 new files in `src/components/editor/blocks/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-block-editor-refactor/3-CONTEXT.md
@.planning/phases/03-block-editor-refactor/3-RESEARCH.md
@src/components/editor/BlockEditor.tsx
@src/types/smart-link.ts

<interfaces>
<!-- Exact line ranges for each block type from RESEARCH.md line map -->

MediaBlockEditor source locations in BlockEditor.tsx:
- Lines 446-465: image (ImageUploader, borderRadius Slider)
- Lines 467-477: video (URL Input)
- Lines 916-942: spotify (spotifyUrl Input, spotifyCompact checkbox)
- Lines 944-982: map (mapAddress Input, mapUrl Textarea, mapHeight Slider)
- Lines 984-1010: html (htmlContent Textarea, htmlHeight Slider)
- Lines 1012-1051: carousel (slide grid + ImageUploader add + autoplay checkbox)

InteractiveBlockEditor source locations in BlockEditor.tsx:
- Lines 492-513: badges (Input with badge parsing)
- Lines 514-589: image-button (ImageUploader, buttonHeight Slider, url/page link toggle with Select)
- Lines 595-617: countdown (datetime-local Input, label Input)
- Lines 874-914: email-capture (content, emailPlaceholder, emailButtonLabel, emailSuccessMessage)
- Lines 1053-1268: animated-button (animStyle grid, content, animSubtitle, animButtonLabel, url/page toggle, animTitleSize Slider, animButtonHeight Slider, animPrimaryColor, animSecondaryColor, gradient preview swatch)

ListBlockEditor source locations in BlockEditor.tsx:
- Lines 619-670: faq (mapped FaqItem editors with Q&A inputs + add button)
- Lines 672-702: gallery (image grid + ImageUploader add)
- Lines 704-758: testimonial (name, role, content, rating stars, avatar ImageUploader)
- Lines 760-802: stats (mapped StatItem editors + add button)
- Lines 804-872: product (ImageUploader, name, price, oldPrice, description, buttonLabel, buttonUrl)
- Lines 1320-1459: contacts (IIFE pattern with mode toggle, up to 2 contact cards)

From src/components/editor/ImageUploader.tsx:
```typescript
export function ImageUploader({ value, onChange, aspectRatio, label, compact }: ImageUploaderProps);
export function buildButtonPresets(buttonHeight: number): Array<{ label: string; height: number }>;
```

Props interface shared by all group editors (per D-03):
```typescript
interface GroupEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
  pages?: SubPage[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MediaBlockEditor for image, video, spotify, map, html, carousel</name>
  <files>src/components/editor/blocks/MediaBlockEditor.tsx</files>
  <read_first>src/components/editor/BlockEditor.tsx (lines 446-477 for image/video, lines 916-1051 for spotify/map/html/carousel), src/types/smart-link.ts (CarouselSlide type)</read_first>
  <action>
Create named export `MediaBlockEditor` wrapped in `memo()`:
```typescript
export const MediaBlockEditor = memo(function MediaBlockEditor({ block, onUpdate }: MediaBlockEditorProps) { ... });
```

Props interface:
```typescript
interface MediaBlockEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
}
```

Render body: lift JSX verbatim from BlockEditor.tsx, wrapped in `<>...</>` Fragment. Each block type as a conditional:

1. `{block.type === "image" && (...)}` — ImageUploader for imageUrl, Slider for borderRadius (lines 446-465)
2. `{block.type === "video" && (...)}` — Input for videoUrl (lines 467-477)
3. `{block.type === "spotify" && (...)}` — Input for spotifyUrl, checkbox for spotifyCompact (lines 916-942)
4. `{block.type === "map" && (...)}` — Input for mapAddress, Textarea for mapUrl, Slider for mapHeight (lines 944-982)
5. `{block.type === "html" && (...)}` — Textarea for htmlContent, Slider for htmlHeight (lines 984-1010)
6. `{block.type === "carousel" && (...)}` — slide grid with delete, ImageUploader add, checkbox for autoplay (lines 1012-1051). Uses `CarouselSlide` type from `@/types/smart-link`.

Imports needed:
- `React, { memo }` from "react"
- `LinkBlock, CarouselSlide` from "@/types/smart-link"
- `Input` from "@/components/ui/input"
- `Label` from "@/components/ui/label"
- `Textarea` from "@/components/ui/textarea"
- `Slider` from "@/components/ui/slider"
- `ImageUploader` from "../ImageUploader"
- `Trash2` from "lucide-react" (used in carousel slide delete)

Per D-13: no logic changes. Per D-14: onUpdate passed directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const MediaBlockEditor = memo" src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "image"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "video"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "spotify"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "map"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "html"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep 'block.type === "carousel"' src/components/editor/blocks/MediaBlockEditor.tsx returns 1 match
    - grep "as any" src/components/editor/blocks/MediaBlockEditor.tsx returns 0 matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>MediaBlockEditor handles all 6 media/embed block types, compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Create InteractiveBlockEditor and ListBlockEditor group editors</name>
  <files>src/components/editor/blocks/InteractiveBlockEditor.tsx, src/components/editor/blocks/ListBlockEditor.tsx</files>
  <read_first>src/components/editor/BlockEditor.tsx (lines 492-617 for badges/image-button/countdown, lines 874-914 for email-capture, lines 1053-1268 for animated-button, lines 619-872 for faq/gallery/testimonial/stats/product, lines 1320-1459 for contacts), src/components/editor/blocks/constants.ts (for ANIM_STYLE_OPTIONS import path)</read_first>
  <action>
**InteractiveBlockEditor.tsx** — handles: `image-button`, `animated-button`, `email-capture`, `countdown`, `badges` (per D-02 group 4).

Create named export `InteractiveBlockEditor` wrapped in `memo()`:
```typescript
export const InteractiveBlockEditor = memo(function InteractiveBlockEditor({ block, onUpdate, pages }: InteractiveBlockEditorProps) { ... });
```

Props interface:
```typescript
interface InteractiveBlockEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
  pages?: SubPage[];
}
```

IMPORTANT — `buttonPresets` memo: Move the `useMemo(() => buildButtonPresets(block.buttonHeight ?? 110), [block.buttonHeight])` from SortableBlock into this component. It uses only `block.buttonHeight` which is available from the `block` prop. Import `buildButtonPresets` from `"../ImageUploader"`.

Render body: lift JSX verbatim from BlockEditor.tsx:
1. `{block.type === "badges" && (...)}` — Input with badge parsing (lines 492-513)
2. `{block.type === "image-button" && (...)}` — ImageUploader, buttonHeight Slider with presets, url/page link toggle using Select (lines 514-589). Uses `buttonPresets` variable and `pages` prop.
3. `{block.type === "countdown" && (...)}` — datetime-local Input, label Input (lines 595-617)
4. `{block.type === "email-capture" && (...)}` — content, emailPlaceholder, emailButtonLabel, emailSuccessMessage Inputs (lines 874-914)
5. `{block.type === "animated-button" && (...)}` — animStyle grid using `ANIM_STYLE_OPTIONS`, content, animSubtitle, animButtonLabel, url/page toggle, animTitleSize Slider, animButtonHeight Slider, animPrimaryColor, animSecondaryColor color pickers, gradient preview swatch (lines 1053-1268). Uses `pages` prop.

Imports needed:
- `React, { memo, useMemo }` from "react"
- `LinkBlock, SubPage` from "@/types/smart-link"
- `Input` from "@/components/ui/input"
- `Label` from "@/components/ui/label"
- `Slider` from "@/components/ui/slider"
- `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from "@/components/ui/select"
- `ImageUploader, buildButtonPresets` from "../ImageUploader"
- `ANIM_STYLE_OPTIONS` from "./constants"

**ListBlockEditor.tsx** — handles: `faq`, `gallery`, `testimonial`, `stats`, `product`, `contacts` (per D-02 group 5).

Create named export `ListBlockEditor` wrapped in `memo()`:
```typescript
export const ListBlockEditor = memo(function ListBlockEditor({ block, onUpdate }: ListBlockEditorProps) { ... });
```

Props interface:
```typescript
interface ListBlockEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
}
```

Render body: lift JSX verbatim from BlockEditor.tsx:
1. `{block.type === "faq" && (...)}` — mapped FaqItem editors with Q&A inputs + add button (lines 619-670)
2. `{block.type === "gallery" && (...)}` — image grid + ImageUploader add (lines 672-702)
3. `{block.type === "testimonial" && (...)}` — name, role, content, rating stars, avatar ImageUploader (lines 704-758)
4. `{block.type === "stats" && (...)}` — mapped StatItem editors + add button (lines 760-802)
5. `{block.type === "product" && (...)}` — ImageUploader, name, price, oldPrice, description, buttonLabel, buttonUrl (lines 804-872)
6. `{block.type === "contacts" && (...)}` — IIFE pattern with mode toggle, up to 2 contact cards each with photo/name/role/whatsapp inputs (lines 1320-1459). Lift IIFE verbatim per D-13 (no logic changes).

Imports needed:
- `React, { memo }` from "react"
- `LinkBlock, FaqItem, GalleryImage, StatItem, ContactItem` from "@/types/smart-link"
- `Input` from "@/components/ui/input"
- `Label` from "@/components/ui/label"
- `Textarea` from "@/components/ui/textarea"
- `Slider` from "@/components/ui/slider"
- `ImageUploader` from "../ImageUploader"
- `Trash2` from "lucide-react"

Per D-13: no logic changes, especially the contacts IIFE. Per D-14: onUpdate passed directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const InteractiveBlockEditor = memo" src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep "export const ListBlockEditor = memo" src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "image-button"' src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep 'block.type === "animated-button"' src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep 'block.type === "email-capture"' src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep 'block.type === "countdown"' src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep 'block.type === "badges"' src/components/editor/blocks/InteractiveBlockEditor.tsx returns 1 match
    - grep 'block.type === "faq"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "gallery"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "testimonial"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "stats"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "product"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep 'block.type === "contacts"' src/components/editor/blocks/ListBlockEditor.tsx returns 1 match
    - grep "buildButtonPresets" src/components/editor/blocks/InteractiveBlockEditor.tsx returns at least 2 matches (import + usage)
    - grep "ANIM_STYLE_OPTIONS" src/components/editor/blocks/InteractiveBlockEditor.tsx returns at least 2 matches (import + usage)
    - grep "as any" src/components/editor/blocks/InteractiveBlockEditor.tsx returns 0 matches
    - grep "as any" src/components/editor/blocks/ListBlockEditor.tsx returns 0 matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>InteractiveBlockEditor handles 5 interactive types with buttonPresets and ANIM_STYLE_OPTIONS, ListBlockEditor handles 6 list types including contacts IIFE, both compile clean</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 — all 3 new files compile alongside unchanged BlockEditor.tsx
- `npx vitest run` passes — no existing tests broken
- All 3 files exist under `src/components/editor/blocks/`
- No `as any` in any new file
- All 17 block types accounted for across 5 group editors (3 in text, 6 in media, 3 in layout, 5 in interactive, 6 in list = 23 block type conditionals, matching the 23 non-hero/info/button types)
</verification>

<success_criteria>
- MediaBlockEditor.tsx handles: image, video, spotify, map, html, carousel
- InteractiveBlockEditor.tsx handles: image-button, animated-button, email-capture, countdown, badges
- ListBlockEditor.tsx handles: faq, gallery, testimonial, stats, product, contacts
- buttonPresets computed inside InteractiveBlockEditor (not passed as prop)
- ANIM_STYLE_OPTIONS imported from ./constants in InteractiveBlockEditor
- npx tsc --noEmit: 0 errors
- npx vitest run: all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-block-editor-refactor/03-02-SUMMARY.md`
</output>
