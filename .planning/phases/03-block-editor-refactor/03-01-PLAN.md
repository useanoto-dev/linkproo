---
phase: 03-block-editor-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/blocks/constants.ts
  - src/components/editor/blocks/unified-items.ts
  - src/components/editor/blocks/TextBlockEditor.tsx
  - src/components/editor/blocks/LayoutBlockEditor.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Constants BLOCK_LABELS, ANIM_STYLE_OPTIONS, BUSINESS_NAME_SIZE_OPTIONS, BUSINESS_NAME_ALIGN_OPTIONS are importable from blocks/constants.ts"
    - "getUnifiedItems and getUnifiedItemsForMode are importable from blocks/unified-items.ts"
    - "TextBlockEditor renders text, cta, and header block types with correct JSX"
    - "LayoutBlockEditor renders spacer, separator, and banner block types with correct JSX"
  artifacts:
    - path: "src/components/editor/blocks/constants.ts"
      provides: "BLOCK_LABELS, ANIM_STYLE_OPTIONS, BUSINESS_NAME_SIZE_OPTIONS, BUSINESS_NAME_ALIGN_OPTIONS"
      exports: ["BLOCK_LABELS", "ANIM_STYLE_OPTIONS", "BUSINESS_NAME_SIZE_OPTIONS", "BUSINESS_NAME_ALIGN_OPTIONS"]
    - path: "src/components/editor/blocks/unified-items.ts"
      provides: "UnifiedItem, getUnifiedItems, getUnifiedItemsForMode, SubPageMode"
      exports: ["UnifiedItem", "getUnifiedItems", "getUnifiedItemsForMode", "SubPageMode"]
    - path: "src/components/editor/blocks/TextBlockEditor.tsx"
      provides: "TextBlockEditor component for text, cta, header"
      exports: ["TextBlockEditor"]
    - path: "src/components/editor/blocks/LayoutBlockEditor.tsx"
      provides: "LayoutBlockEditor component for spacer, separator, banner"
      exports: ["LayoutBlockEditor"]
  key_links:
    - from: "src/components/editor/blocks/TextBlockEditor.tsx"
      to: "@/types/smart-link"
      via: "import LinkBlock"
      pattern: "import.*LinkBlock.*from.*@/types/smart-link"
    - from: "src/components/editor/blocks/constants.ts"
      to: "@/types/smart-link"
      via: "import BlockType"
      pattern: "import.*BlockType.*from.*@/types/smart-link"
---

<objective>
Extract shared constants, utility functions, and the two smallest group editors (TextBlockEditor, LayoutBlockEditor) from the BlockEditor.tsx monolith into separate files under `src/components/editor/blocks/`.

Purpose: Foundation files that Plan 02 (larger group editors) and Plan 03 (dispatcher wiring) depend on. Constants and utilities must exist before the dispatcher can import them.
Output: 4 new files in `src/components/editor/blocks/`. BlockEditor.tsx is NOT modified yet — that happens in Plan 03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-block-editor-refactor/3-CONTEXT.md
@.planning/phases/03-block-editor-refactor/3-RESEARCH.md
@src/components/editor/BlockEditor.tsx
@src/types/smart-link.ts

<interfaces>
<!-- Key types the executor needs from smart-link.ts -->

From src/types/smart-link.ts:
- LinkBlock (block data with type, content, and all optional fields)
- BlockType (union of all block type strings)
- SubPage (sub-page structure with blocks array)
- SmartLink (top-level link object with buttons, blocks, pages)
- SmartLinkButton (button data)
- FaqItem, GalleryImage, StatItem, ContactItem, CarouselSlide (nested item types)

From src/components/editor/BlockEditor.tsx (lines 126-131):
```typescript
interface SubPageMode {
  page: SubPage;
  onUpdatePage: (updates: Partial<SubPage>) => void;
  onAddBlock: (type: BlockType, defaults?: Record<string, unknown>) => void;
  onInsertBlockAt: (type: BlockType, atIndex: number, defaults?: Record<string, unknown>) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.ts and unified-items.ts extraction files</name>
  <files>src/components/editor/blocks/constants.ts, src/components/editor/blocks/unified-items.ts</files>
  <read_first>src/components/editor/BlockEditor.tsx (lines 1-175), src/types/smart-link.ts</read_first>
  <action>
Create directory `src/components/editor/blocks/`.

**constants.ts** (per D-09): Extract these 4 constants verbatim from BlockEditor.tsx:
- `BLOCK_LABELS: Record<BlockType, string>` (lines 36-63, 24 entries — hero through contacts)
- `ANIM_STYLE_OPTIONS` (lines 65-76, 10 entries — whatsapp through telegram, with `as const`)
- `BUSINESS_NAME_SIZE_OPTIONS` (lines 78-84, 5 entries — XS through XG)
- `BUSINESS_NAME_ALIGN_OPTIONS` (lines 86-90, 3 entries — left, center, right with `as const`)

Import `BlockType` from `@/types/smart-link`. Import `AlignLeft`, `AlignCenter`, `AlignRight` from `lucide-react` (needed by BUSINESS_NAME_ALIGN_OPTIONS).

All constants use `export const`. No default export.

**unified-items.ts** (per D-10): Extract these items verbatim from BlockEditor.tsx:
- `SubPageMode` interface (lines 126-131) — export it
- `UnifiedItem` type (lines 143-145) — export it
- `getUnifiedItems` function (lines 147-161) — export it
- `getUnifiedItemsForMode` function (lines 163-174) — export it

Import `SmartLink`, `SmartLinkButton`, `LinkBlock`, `BlockType`, `SubPage` from `@/types/smart-link`.

All exports are named. No default export.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const BLOCK_LABELS" src/components/editor/blocks/constants.ts returns 1 match
    - grep "export const ANIM_STYLE_OPTIONS" src/components/editor/blocks/constants.ts returns 1 match
    - grep "export const BUSINESS_NAME_SIZE_OPTIONS" src/components/editor/blocks/constants.ts returns 1 match
    - grep "export const BUSINESS_NAME_ALIGN_OPTIONS" src/components/editor/blocks/constants.ts returns 1 match
    - grep "export function getUnifiedItems" src/components/editor/blocks/unified-items.ts returns 1 match
    - grep "export function getUnifiedItemsForMode" src/components/editor/blocks/unified-items.ts returns 1 match
    - grep "export interface SubPageMode" src/components/editor/blocks/unified-items.ts returns 1 match
    - grep "export type UnifiedItem" src/components/editor/blocks/unified-items.ts returns 1 match
    - npx tsc --noEmit exits 0 (these new files must compile clean alongside existing BlockEditor.tsx which still has its own copies)
  </acceptance_criteria>
  <done>constants.ts exports 4 constants, unified-items.ts exports SubPageMode + UnifiedItem + 2 functions, tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Create TextBlockEditor and LayoutBlockEditor group editors</name>
  <files>src/components/editor/blocks/TextBlockEditor.tsx, src/components/editor/blocks/LayoutBlockEditor.tsx</files>
  <read_first>src/components/editor/BlockEditor.tsx (lines 209-267 for SortableBlock state, lines 311-444 for text/cta/header JSX, lines 479-490 for spacer, lines 591-593 for separator, lines 1271-1318 for banner), src/types/smart-link.ts</read_first>
  <action>
**TextBlockEditor.tsx** — handles block types: `text`, `cta`, `header` (per D-02 group 1).

Create named export `TextBlockEditor` wrapped in `memo()` following the ButtonBlockEditor pattern:
```typescript
export const TextBlockEditor = memo(function TextBlockEditor({ block, onUpdate, textareaRef, applyTextFormat }: TextBlockEditorProps) { ... });
```

Props interface (per D-03, D-15):
```typescript
interface TextBlockEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
  textareaRef: React.RefObject<HTMLTextAreaElement>;
  applyTextFormat: (tag: string, url?: string) => void;
}
```

Render body: lift JSX verbatim from BlockEditor.tsx:
- Lines 312-395: `{(block.type === "text" || block.type === "cta") && (...)}` — the format toolbar (B/I/Link buttons calling applyTextFormat), Textarea with ref={textareaRef}, subtitle Input, blockTextColor color picker, blockTextAlign buttons
- Lines 397-444: `{block.type === "header" && (...)}` — Input for content, emoji Input, blockTextColor, blockTextAlign

Wrap both conditionals in a `<>...</>` Fragment.

Imports needed: `React, { memo, RefObject }` from "react", `LinkBlock` from "@/types/smart-link", `Input` from "@/components/ui/input", `Label` from "@/components/ui/label", `Textarea` from "@/components/ui/textarea", `AlignLeft, AlignCenter, AlignRight` from "lucide-react".

Verify: the `textareaRef` is attached to the `<Textarea ref={textareaRef}>` element inside the text/cta conditional (this is critical for applyTextFormat to work per D-15).

**LayoutBlockEditor.tsx** — handles block types: `spacer`, `separator`, `banner` (per D-02 group 3).

Create named export `LayoutBlockEditor` wrapped in `memo()`:
```typescript
export const LayoutBlockEditor = memo(function LayoutBlockEditor({ block, onUpdate }: LayoutBlockEditorProps) { ... });
```

Props interface (per D-03):
```typescript
interface LayoutBlockEditorProps {
  block: LinkBlock;
  onUpdate: (id: string, updates: Partial<LinkBlock>) => void;
}
```

Render body: lift JSX verbatim from BlockEditor.tsx:
- Lines 479-490: `{block.type === "spacer" && (...)}` — height Slider
- Lines 591-593: `{block.type === "separator" && (...)}` — static text
- Lines 1271-1318: `{block.type === "banner" && (...)}` — bannerTag Input, content Textarea, subtitle Input, bannerBg color picker

Imports needed: `React, { memo }` from "react", `LinkBlock` from "@/types/smart-link", `Input` from "@/components/ui/input", `Label` from "@/components/ui/label", `Textarea` from "@/components/ui/textarea", `Slider` from "@/components/ui/slider".

Per D-13: no logic changes. Lift JSX verbatim. Per D-14: onUpdate passed directly, no wrappers.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const TextBlockEditor = memo" src/components/editor/blocks/TextBlockEditor.tsx returns 1 match
    - grep "export const LayoutBlockEditor = memo" src/components/editor/blocks/LayoutBlockEditor.tsx returns 1 match
    - grep "textareaRef" src/components/editor/blocks/TextBlockEditor.tsx returns at least 2 matches (prop + ref assignment)
    - grep "applyTextFormat" src/components/editor/blocks/TextBlockEditor.tsx returns at least 2 matches (prop + usage)
    - grep 'block.type === "spacer"' src/components/editor/blocks/LayoutBlockEditor.tsx returns 1 match
    - grep 'block.type === "separator"' src/components/editor/blocks/LayoutBlockEditor.tsx returns 1 match
    - grep 'block.type === "banner"' src/components/editor/blocks/LayoutBlockEditor.tsx returns 1 match
    - grep "as any" src/components/editor/blocks/TextBlockEditor.tsx returns 0 matches
    - grep "as any" src/components/editor/blocks/LayoutBlockEditor.tsx returns 0 matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TextBlockEditor handles text/cta/header, LayoutBlockEditor handles spacer/separator/banner, both compile clean with no `as any`</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 — all 4 new files compile alongside unchanged BlockEditor.tsx
- `npx vitest run` passes — no existing tests broken (none import from BlockEditor.tsx)
- All 4 files exist under `src/components/editor/blocks/`
- No `as any` in any new file
</verification>

<success_criteria>
- 4 new files created in src/components/editor/blocks/
- constants.ts exports: BLOCK_LABELS, ANIM_STYLE_OPTIONS, BUSINESS_NAME_SIZE_OPTIONS, BUSINESS_NAME_ALIGN_OPTIONS
- unified-items.ts exports: SubPageMode, UnifiedItem, getUnifiedItems, getUnifiedItemsForMode
- TextBlockEditor.tsx exports memo component handling text, cta, header
- LayoutBlockEditor.tsx exports memo component handling spacer, separator, banner
- npx tsc --noEmit: 0 errors
- npx vitest run: all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-block-editor-refactor/03-01-SUMMARY.md`
</output>
