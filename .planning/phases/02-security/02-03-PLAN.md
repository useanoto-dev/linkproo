---
phase: 02-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SettingsPage.tsx
  - src/components/DashboardLayout.tsx
autonomous: true
requirements: [D-18, D-19, D-20, D-21]

must_haves:
  truths:
    - "User sees fingerprint disclosure text in SettingsPage"
    - "Disclosure mentions LGPD Art. 7, IX and legitimate interest"
    - "DashboardLayout has a privacy policy link in the footer"
    - "No consent pop-up or banner is shown (D-20)"
  artifacts:
    - path: "src/pages/SettingsPage.tsx"
      provides: "Privacy/Security section with LGPD disclosure text"
      contains: "Privacidade e Seguranca"
    - path: "src/components/DashboardLayout.tsx"
      provides: "Footer with privacy policy link"
      contains: "Politica de Privacidade"
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "lucide-react"
      via: "Shield icon import"
      pattern: "Shield"
    - from: "src/components/DashboardLayout.tsx"
      to: "/privacy"
      via: "Footer anchor link"
      pattern: "href.*privacy"
---

<objective>
Add LGPD fingerprint disclosure section to SettingsPage and a privacy policy link to DashboardLayout footer.

Purpose: Close the 5th security gap — LGPD compliance for device fingerprinting disclosure.
Output: SettingsPage with privacy section, DashboardLayout with footer link.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security/2-CONTEXT.md
@.planning/phases/02-security/2-RESEARCH.md

<interfaces>
<!-- SettingsPage.tsx structure: -->
<!-- Imports: User, Building2, Save, Loader2, Camera from lucide-react -->
<!-- Layout: DashboardLayout wrapping max-w-2xl mx-auto space-y-6 -->
<!-- Sections: Profile (motion.div), Company (motion.div), Save button (motion.button) -->
<!-- Save button is the LAST element in space-y-6, at ~line 232-243 -->

<!-- DashboardLayout.tsx structure: -->
<!-- SidebarProvider > div.min-h-screen > AppSidebar + div.flex-1 > header + main -->
<!-- main contains {children}, no footer exists -->
<!-- useDeviceFingerprint() called at top of component -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LGPD disclosure to SettingsPage + footer to DashboardLayout</name>
  <files>src/pages/SettingsPage.tsx, src/components/DashboardLayout.tsx</files>
  <read_first>src/pages/SettingsPage.tsx, src/components/DashboardLayout.tsx</read_first>
  <action>
**Per D-18, D-19, D-20, D-21:**

1. **Edit `src/pages/SettingsPage.tsx`:**

   a. Add `Shield` to the lucide-react import. Change line 6 from:
   ```typescript
   import { User, Building2, Save, Loader2, Camera } from "lucide-react";
   ```
   to:
   ```typescript
   import { User, Building2, Save, Loader2, Camera, Shield } from "lucide-react";
   ```

   b. Insert a new `motion.div` section BETWEEN the Company section closing `</motion.div>` (~line 229) and the Save `<motion.button>` (~line 232). The new section:

   ```tsx
        {/* Privacy & Security — LGPD disclosure (per D-18) */}
        <motion.div
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.15 }}
          className="rounded-xl border border-border bg-card p-5 space-y-3"
        >
          <div className="flex items-center gap-2 mb-1">
            <Shield className="h-4 w-4 text-primary" />
            <h2 className="text-sm font-semibold text-foreground">Privacidade e Segurança</h2>
          </div>
          <p className="text-xs text-muted-foreground leading-relaxed">
            Para proteger sua conta contra acesso não autorizado, coletamos um identificador
            técnico do dispositivo que inclui informações do navegador (userAgent, idioma,
            fuso horário), resolução de tela e características gráficas (canvas, WebGL).
            Esses dados são usados exclusivamente para segurança da conta (base legal:
            legítimo interesse, Art. 7º, IX da LGPD) e não são compartilhados com terceiros
            para fins publicitários.
          </p>
          <a
            href="/privacy"
            className="text-xs text-primary hover:underline"
            target="_blank"
            rel="noopener noreferrer"
          >
            Consultar Política de Privacidade →
          </a>
        </motion.div>
   ```

   c. Update the Save button transition delay from `delay: 0.15` to `delay: 0.2` so it animates after the new privacy section.

2. **Edit `src/components/DashboardLayout.tsx`** (per D-21):

   Add a footer inside the `<main>` wrapper. The current structure is:
   ```tsx
   <main className={noPadding ? "flex-1 min-h-0" : "flex-1 p-4 sm:p-6"}>
     {children}
   </main>
   ```

   Change the `<main>` and its content to include a footer AFTER `{children}`. The main must use flex column to push footer down:

   ```tsx
   <main className={noPadding ? "flex-1 flex flex-col min-h-0" : "flex-1 flex flex-col p-4 sm:p-6"}>
     <div className={noPadding ? "flex-1 min-h-0" : "flex-1"}>
       {children}
     </div>
     <footer className="mt-auto pt-6 pb-2 px-4 text-center">
       <a
         href="/privacy"
         target="_blank"
         rel="noopener noreferrer"
         className="text-xs text-muted-foreground hover:text-foreground transition-colors"
       >
         Política de Privacidade
       </a>
     </footer>
   </main>
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- `src/pages/SettingsPage.tsx` contains `import.*Shield.*from "lucide-react"`
- `src/pages/SettingsPage.tsx` contains the string `Privacidade e Segurança`
- `src/pages/SettingsPage.tsx` contains `Art. 7º, IX da LGPD`
- `src/pages/SettingsPage.tsx` contains `legítimo interesse`
- `src/pages/SettingsPage.tsx` contains `href="/privacy"`
- `src/pages/SettingsPage.tsx` contains `<Shield`
- `src/pages/SettingsPage.tsx` does NOT contain a consent banner or pop-up (per D-20)
- `src/components/DashboardLayout.tsx` contains `<footer`
- `src/components/DashboardLayout.tsx` contains `Política de Privacidade`
- `src/components/DashboardLayout.tsx` contains `href="/privacy"`
- `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>LGPD disclosure visible in SettingsPage privacy section with Shield icon, LGPD article reference, and privacy policy link. DashboardLayout has footer with privacy policy link.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with 0 errors
2. `grep "Shield" src/pages/SettingsPage.tsx` returns match
3. `grep "LGPD" src/pages/SettingsPage.tsx` returns match
4. `grep "footer" src/components/DashboardLayout.tsx` returns match
5. `grep "privacy" src/components/DashboardLayout.tsx` returns match
</verification>

<success_criteria>
- SettingsPage has a "Privacidade e Seguranca" section with Shield icon
- Disclosure text mentions: userAgent, idioma, fuso horario, canvas, WebGL
- Disclosure references LGPD Art. 7, IX and "legitimo interesse"
- Privacy policy link (/privacy) appears in SettingsPage section
- DashboardLayout has a footer with "Politica de Privacidade" link
- No consent pop-up or banner added (per D-20)
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-security/02-03-SUMMARY.md`
</output>
