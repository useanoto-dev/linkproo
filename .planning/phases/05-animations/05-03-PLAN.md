---
phase: 05-animations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LinksPage.tsx
  - src/pages/AnalyticsPage.tsx
  - src/pages/Dashboard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "LinksPage empty state (no links) shows animated Lucide icon with float + fade-up entry + CTA button"
    - "AnalyticsPage shows animated empty state when analytics data is null/no links"
    - "Dashboard EmptyState for template search uses motion.div fade-up and Lucide icon with float"
  artifacts:
    - path: "src/pages/LinksPage.tsx"
      provides: "Animated empty state with floating Lucide icon"
      contains: "animate={{ y: [0, -6, 0] }}"
    - path: "src/pages/AnalyticsPage.tsx"
      provides: "Animated empty state for no-data scenario"
      contains: "animate={{ y: [0, -6, 0] }}"
    - path: "src/pages/Dashboard.tsx"
      provides: "Upgraded EmptyState with animation"
      contains: "motion.div"
  key_links:
    - from: "src/pages/LinksPage.tsx"
      to: "framer-motion"
      via: "motion.div for fade-up + keyframe float"
      pattern: "motion\\.div"
---

<objective>
Upgrade empty states in LinksPage, AnalyticsPage, and Dashboard with animated Lucide icons and fade-up entry animations.

Purpose: Per D-08/D-09/D-10/D-11, empty states use Lucide icon (48px) + heading + subtext + optional CTA, with fade-up entry and floating icon animation. No external illustration libraries.

Output: Three pages with polished animated empty states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-animations/5-CONTEXT.md
@.planning/phases/05-animations/5-RESEARCH.md
@src/pages/LinksPage.tsx
@src/pages/AnalyticsPage.tsx
@src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade LinksPage empty state with animated Lucide icon</name>
  <files>src/pages/LinksPage.tsx</files>
  <read_first>
    - src/pages/LinksPage.tsx (full file — find the `links.length === 0` empty state block around line 258-283)
  </read_first>
  <action>
Replace the `links.length === 0` empty state block (the `<div className="flex flex-col items-center justify-center py-20 text-center">` section, approximately lines 259-283) with an animated version per D-08/D-09/D-10/D-11.

The file already imports `motion` from framer-motion and `Inbox` from lucide-react, so no new imports needed.

Replace the static empty state with:

```tsx
<motion.div
  initial={{ opacity: 0, y: 16 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.4, ease: "easeOut" }}
  className="flex flex-col items-center justify-center py-20 text-center"
>
  <motion.div
    animate={{ y: [0, -6, 0] }}
    transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }}
    className="w-16 h-16 rounded-2xl bg-secondary flex items-center justify-center mb-4"
  >
    <Inbox className="h-8 w-8 text-muted-foreground" />
  </motion.div>
  <h3 className="font-display font-semibold text-foreground mb-1">Nenhum link criado</h3>
  <p className="text-sm text-muted-foreground mb-4 max-w-sm">
    Crie seu primeiro link usando um modelo ou comece do zero
  </p>
  <div className="flex gap-3">
    <button
      type="button"
      onClick={() => navigate("/")}
      className="px-4 py-2 rounded-lg bg-secondary border border-border text-foreground text-sm font-medium hover:bg-secondary/80 transition-colors cursor-pointer select-none"
    >
      Ver Modelos
    </button>
    <button
      type="button"
      onClick={() => navigate("/links/new")}
      className="px-4 py-2 rounded-lg bg-primary text-primary-foreground text-sm font-medium hover:opacity-90 transition-opacity cursor-pointer select-none"
    >
      Criar do Zero
    </button>
  </div>
</motion.div>
```

Key changes from the original:
- Outer `div` becomes `motion.div` with `initial={{ opacity: 0, y: 16 }}` and `animate={{ opacity: 1, y: 0 }}` (D-09 fade-up values)
- Icon container becomes `motion.div` with `animate={{ y: [0, -6, 0] }}` keyframe float (D-11)
- Float has `duration: 3, repeat: Infinity, ease: "easeInOut"`
- All existing text/buttons preserved exactly
  </action>
  <verify>
    <automated>grep -c "animate={{ y: \[0, -6, 0\] }}" src/pages/LinksPage.tsx</automated>
  </verify>
  <done>LinksPage empty state animates with fade-up entry and floating Lucide icon per D-08/D-09/D-11</done>
</task>

<task type="auto">
  <name>Task 2: Add animated empty state to AnalyticsPage + upgrade Dashboard EmptyState</name>
  <files>src/pages/AnalyticsPage.tsx, src/pages/Dashboard.tsx</files>
  <read_first>
    - src/pages/AnalyticsPage.tsx (full file — find where `data?.analytics` is null or links are empty, around line 51-56 and the chart empty states)
    - src/pages/Dashboard.tsx (full file — find the EmptyState component around line 146-163)
  </read_first>
  <action>
**AnalyticsPage.tsx:**

The page currently handles "no data" by returning early with `analytics: null` when links are empty (line 51). After the loading skeleton, if `data?.analytics` is null, there is no dedicated empty state — it just shows stat cards with 0 values and empty charts.

Add a full-page empty state when `data?.analytics` is null (no links exist). Insert this check right after the loading skeleton block (after `isLoading ? (...)`) and before the main content:

After the `isLoading ? (` block, add an additional condition. Restructure the ternary to:

```tsx
{isLoading ? (
  /* existing skeleton */
) : !data?.analytics ? (
  <motion.div
    initial={{ opacity: 0, y: 16 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.4, ease: "easeOut" }}
    className="flex flex-col items-center justify-center py-20 text-center"
  >
    <motion.div
      animate={{ y: [0, -6, 0] }}
      transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }}
      className="w-16 h-16 rounded-2xl bg-secondary flex items-center justify-center mb-4"
    >
      <Eye className="h-8 w-8 text-muted-foreground" />
    </motion.div>
    <h3 className="font-semibold text-foreground mb-1">Ainda sem visitas</h3>
    <p className="text-sm text-muted-foreground mb-4 max-w-sm">
      Crie e compartilhe seu primeiro link para começar a ver analytics aqui
    </p>
  </motion.div>
) : (
  /* existing stat cards + charts content */
)}
```

`Eye` is already imported from lucide-react. `motion` is already imported.

**Dashboard.tsx:**

The `EmptyState` component (lines 146-163) currently shows a static `div` with emoji `🔍` and text. Upgrade it:

1. Add import for `motion` if not already imported (it is — line 1).
2. Add import for a Lucide icon: `SearchX` from lucide-react (or use `Search` which is not currently imported — use `Layout` which is already imported, or import `SearchX`). Since the empty state is about "no templates in this category", import `FolderSearch` from lucide-react.

Actually, checking imports — `Layout` is already imported. For the "no template found" context, use the existing `Layout` icon or import `FolderOpen`. Let me keep it simple and use `Layout` which is already imported.

Replace the EmptyState component:

```tsx
function EmptyState({ onReset }: { onReset: () => void }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 16 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4, ease: "easeOut" }}
      className="flex flex-col items-center justify-center py-24 text-center"
    >
      <motion.div
        animate={{ y: [0, -6, 0] }}
        transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }}
        className="w-16 h-16 rounded-2xl bg-secondary flex items-center justify-center mb-4"
      >
        <Layout className="h-8 w-8 text-muted-foreground" />
      </motion.div>
      <p className="text-base font-semibold text-foreground mb-2">Nenhum modelo nesta categoria</p>
      <p className="text-sm text-muted-foreground max-w-xs">
        Tente selecionar outra categoria ou visualize todos os modelos disponíveis.
      </p>
      <button
        onClick={onReset}
        className="mt-4 px-4 py-2 rounded-lg bg-primary/10 hover:bg-primary/15 text-primary text-sm font-medium transition-colors cursor-pointer"
      >
        Ver todos os modelos
      </button>
    </motion.div>
  );
}
```

Key changes:
- Outer `div` → `motion.div` with D-09 fade-up values
- Emoji `🔍` → `Layout` Lucide icon (48px) inside floating `motion.div` with D-11 keyframe
- All text and button preserved exactly
  </action>
  <verify>
    <automated>grep -c "animate={{ y: \[0, -6, 0\] }}" src/pages/AnalyticsPage.tsx && grep -c "animate={{ y: \[0, -6, 0\] }}" src/pages/Dashboard.tsx</automated>
  </verify>
  <done>AnalyticsPage has animated empty state for no-data. Dashboard EmptyState upgraded with motion animation and Lucide icon. No emoji icons remain in empty states.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- LinksPage with 0 links shows floating icon + fade-up animated empty state
- AnalyticsPage with no links shows "Ainda sem visitas" animated empty state
- Dashboard template search with no results shows animated EmptyState with Lucide icon
</verification>

<success_criteria>
- All three empty states use motion.div with D-09 fade-up values (opacity 0→1, y 16→0, 0.4s)
- All icon wrappers have D-11 float animation (y: [0, -6, 0], 3s infinite)
- Lucide icons used everywhere, no emoji (D-08)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-animations/05-03-SUMMARY.md`
</output>
